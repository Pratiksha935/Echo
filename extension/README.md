# EchoCheck browser extension — CV1 demo

## Load it locally

1. Open `chrome://extensions` in Chrome.
2. Enable **Developer mode**.
3. Select **Load unpacked**.
4. Choose this `extension` folder.
5. Visit `http://localhost:3000/demo-article`.

The extension analyses that demo article locally and displays the EchoCheck avatar. This CV1 build uses embedded test knowledge; the production version will call the authenticated EchoCheck/Hermes analysis endpoint.
